spring:
  application:
    name: imobiliaria01-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/imobiliaria
    username: postgres
    password: 2174

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true